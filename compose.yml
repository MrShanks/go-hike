services:
  tracks:
    build:
      context: .
      dockerfile: Dockerfile
    image: tracks:local
    ports:
      - "${TRACKS_PORT:-8080}:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
